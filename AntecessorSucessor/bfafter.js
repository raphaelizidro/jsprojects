function start(){
    let n = window.prompt('Type any number')
    let ant = (n - 1)
    let suc = (ant + 2)
    let finalresult = window.alert(`O antecessor de ${n} é ${ant} e o sucessor é ${suc}`)
}
