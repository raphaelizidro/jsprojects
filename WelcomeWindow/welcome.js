function start(){

 let name = window.prompt('What is your name?')
 let age = window.prompt(` Ok, ${name}, how old are you?`)
 let finalresult = window.alert(`I met ${name} with ${age} years old.`)
    
}

